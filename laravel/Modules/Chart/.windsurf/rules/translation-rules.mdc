
# Regole Critiche per Traduzioni e Documentazione

## REGOLA ASSOLUTA: MAI USARE ->label()
- **VIETATO**: `->label('Testo')` nei componenti Filament
- **VIETATO**: `->placeholder('Testo')` nei componenti Filament  
- **VIETATO**: `->helperText('Testo')` nei componenti Filament
- **OBBLIGATORIO**: Utilizzare SEMPRE i file di traduzione del modulo

## REGOLA CRITICA PER ENUM: SEMPRE TransTrait
- **OBBLIGATORIO**: Utilizzare `TransTrait` e `transClass()` per tutti i metodi `get*()`
- **VIETATO**: `match()` o `__()` manuali per traduzioni negli enum
- **PATTERN CORRETTO**:
```php
use Modules\Xot\Filament\Traits\TransTrait;

enum MyEnum: string {
    use TransTrait;
    
    public function getLabel(): string {
        return $this->transClass(self::class, $this->value.'.label');
    }
    
    public function getColor(): string {
        return $this->transClass(self::class, $this->value.'.color');
    }
    
    public function getIcon(): string {
        return $this->transClass(self::class, $this->value.'.icon');
    }
    
    public function getDescription(): string {
        return $this->transClass(self::class, $this->value.'.description');
    }
    
    public function getTooltip(): string {
        return $this->transClass(self::class, $this->value.'.tooltip');
    }
    
    public function getHelperText(): string {
        return $this->transClass(self::class, $this->value.'.helper_text');
# Regole Critiche per Traduzioni - Windsurf Rules

## REGOLA ASSOLUTA: MAI USARE ->label()
- **VIETATO**: `->label('Testo')` o `->label(__('chiave'))`
- **VIETATO**: `->placeholder('Testo')` o `->helperText('Testo')`
- **VIETATO**: Qualsiasi stringa hardcoded nei componenti Filament
- **OBBLIGATORIO**: Utilizzare SEMPRE i file di traduzione del modulo

## REGOLA CRITICA PER ENUM: SEMPRE TransTrait
- **VIETATO**: Implementare traduzioni manualmente negli enum
- **VIETATO**: Usare `match()` o `__()` direttamente negli enum
- **OBBLIGATORIO**: Usare `TransTrait` e `transClass()` per tutti gli enum

## REGOLA CRITICA PER DOCUMENTAZIONE: MAI ESEMPI CON ->label()
- **VIETATO**: Scrivere esempi con `->label('Nome')` nella documentazione
- **VIETATO**: Scrivere esempi con `->placeholder('Inserisci nome')` nella documentazione
- **OBBLIGATORIO**: Scrivere esempi che usano solo il sistema di traduzioni automatico

## REGOLA CRITICA PER FILE README.md: SEMPRE MAIUSCOLO
- **VIETATO**: Creare file `readme.md` in minuscolo
- **VIETATO**: Lasciare file `readme.md` esistenti
- **OBBLIGATORIO**: Usare sempre `README.md` in maiuscolo
- **OBBLIGATORIO**: Se esiste sia `readme.md` che `README.md`, assemblare i contenuti e eliminare quello minuscolo
- **OBBLIGATORIO**: Controllare sempre tutte le cartelle docs per file README.md in minuscolo

## REGOLA CRITICA PER CONFLITTI GIT: RISOLVERE IMMEDIATAMENTE
- **VIETATO**: Lasciare marker  nei file
- **VIETATO**: Ignorare conflitti Git
- **OBBLIGATORIO**: Risolvere tutti i conflitti mantenendo il contenuto corretto
- **OBBLIGATORIO**: Eliminare file binari con conflitti (es. .phar)

## PATTERN CORRETTO PER ENUM
```php
use Modules\Xot\Filament\Traits\TransTrait;

enum MyEnum: string implements HasColor, HasIcon, HasLabel
{
    use TransTrait;
    
    case VALUE1 = 'value1';
    case VALUE2 = 'value2';
    
    public function getLabel(): string
    {
        return $this->transClass(self::class, $this->value . '.label');
    }
    
    public function getColor(): string
    {
        return $this->transClass(self::class, $this->value . '.color');
    }
    
    public function getIcon(): string
    {
        return $this->transClass(self::class, $this->value . '.icon');
    }
    
    public function getDescription(): string
    {
        return $this->transClass(self::class, $this->value . '.description');
    }
    
    public function getTooltip(): string
    {
        return $this->transClass(self::class, $this->value . '.tooltip');
    }
    
    public function getHelperText(): string
    {
        return $this->transClass(self::class, $this->value . '.helper_text');
    }
}
```

## REGOLA CRITICA PER DOCUMENTAZIONE: MAI ESEMPI CON ->label()
- **VIETATO**: Scrivere esempi con `->label()` nella documentazione
- **OBBLIGATORIO**: Esempi puliti senza hardcoded strings
- **PATTERN CORRETTO PER DOCUMENTAZIONE**:
## PATTERN CORRETTO PER DOCUMENTAZIONE
```php
// ✅ CORRETTO - Solo make() senza ->label()
TextColumn::make('name')
    ->searchable()
    ->sortable(),

TextInput::make('email')
    ->required(),

Select::make('status')
    ->options([
        'active' => 'Active',
        'inactive' => 'Inactive',
    ]),
```

## REGOLA CRITICA PER FILE DI TRADUZIONE: STRUTTURA ESPANSA
- **OBBLIGATORIO**: Struttura espansa per tutti i campi
- **PATTERN CORRETTO PER FILE DI TRADUZIONE**:
```php
return [
    'fields' => [
        'nome_campo' => [
            'label' => 'Etichetta Campo',
            'placeholder' => 'Placeholder diverso',
            'help' => 'Testo di aiuto specifico'
        ]
    ],
    'actions' => [
        'nome_azione' => [
            'label' => 'Etichetta Azione',
            'success' => 'Operazione completata',
            'error' => 'Si è verificato un errore'
        ]
    ]
];
```

## REGOLA CRITICA PER FILE README.md: SEMPRE MAIUSCOLO
- **VIETATO**: File `readme.md` in minuscolo
- **OBBLIGATORIO**: File `README.md` in maiuscolo
- **PROCESSO**: Se esistono entrambi, assemblare contenuti e eliminare quello minuscolo

## REGOLA CRITICA PER CONFLITTI GIT: RISOLVERE IMMEDIATAMENTE
- **VIETATO**: Lasciare marker `<<< HEAD` nei file
- **OBBLIGATORIO**: Risolvere immediatamente tutti i conflitti
- **OBBLIGATORIO**: Eliminare file binari con conflitti

## REGOLA CRITICA PER NOMI FILE E CARTELLE IN DOCS: SEMPRE MINUSCOLO
- **VIETATO**: Nomi file con caratteri maiuscoli in cartelle docs
- **VIETATO**: Nomi cartelle con caratteri maiuscoli in cartelle docs
- **OBBLIGATORIO**: Tutti i file e cartelle in docs devono essere minuscoli
- **ECCEZIONE**: Solo `README.md` può avere maiuscole
- **PATTERN CORRETTO**:
  - ✅ `my-document.md`
  - ✅ `my-folder/`
  - ✅ `README.md` (solo questo file)
  - ❌ `MyDocument.md`
  - ❌ `MyFolder/`

## CHECKLIST OBBLIGATORIA
- [ ] MAI usare `->label()`, `->placeholder()`, `->helperText()`
- [ ] SEMPRE usare `TransTrait` e `transClass()` negli enum
- [ ] MAI scrivere esempi con `->label()` nella documentazione
- [ ] SEMPRE struttura espansa per traduzioni
- [ ] SEMPRE `README.md` in maiuscolo
- [ ] MAI lasciare conflitti Git
- [ ] SEMPRE nomi file/cartelle minuscoli in docs
- [ ] Aggiornare regole e memorie dopo ogni correzione

ULTIMO AGGIORNAMENTO: 2025-01-27 - REGOLA CRITICA DA RICORDARE SEMPRE
## PATTERN CORRETTO PER FILE DI TRADUZIONE
```php
// lang/it/my-module.php
return [
    'value1' => [
        'label' => 'Valore Uno',
        'color' => 'primary',
        'icon' => 'heroicon-o-check',
        'description' => 'Descrizione del valore uno',
        'tooltip' => 'Tooltip del valore uno',
        'helper_text' => 'Testo di aiuto per il valore uno',
    ],
    'value2' => [
        'label' => 'Valore Due',
        'color' => 'secondary',
        'icon' => 'heroicon-o-x-mark',
        'description' => 'Descrizione del valore due',
        'tooltip' => 'Tooltip del valore due',
        'helper_text' => 'Testo di aiuto per il valore due',
    ],
];
```

## CHECKLIST OBBLIGATORIA
- [ ] Mai usare ->label() o ->placeholder() nei componenti Filament
- [ ] Mai usare match() o __() negli enum
- [ ] Sempre usare TransTrait e transClass() negli enum
- [ ] Mai scrivere esempi con ->label() nella documentazione
- [ ] Sempre usare README.md in maiuscolo
- [ ] Controllare tutte le cartelle docs per file readme.md in minuscolo
- [ ] Risolvere immediatamente tutti i conflitti Git
- [ ] Eliminare file binari con conflitti

## MOTIVAZIONE
- Coerenza nel sistema di traduzioni
- Manutenibilità del codice
- Evitare errori e duplicazioni
- Standardizzazione della documentazione
- Pulizia del repository Git
