---
trigger: manual
description:
globs:
---
# Processo Dev → Traduttore: Regole e Checklist

## 1. Preparazione
- Prepara i file PHP/JSON di riferimento in `/lang/en/` e `/lang/en.json`.
- Elimina stringhe non usate prima di inviare ai traduttori.

## 2. Istruzioni per i Traduttori
- Nei file PHP: traduci solo il testo a destra di `=>`, non cambiare chiavi o struttura.
- Nei file JSON: traduci solo il valore, non la chiave.
- Non aggiungere, rimuovere o spostare chiavi.
- Se serve un apostrofo (`'`), anteporre `\`.

## 3. Reintegrazione
- Sostituisci i file tradotti nella lingua target e verifica la sintassi.
- Testa l'applicazione in tutte le lingue.

## 4. Modifiche Proposte
- Nei Blade, sostituire tutte le stringhe hardcoded con chiavi strutturate (es. `__('auth.login.submit_button')`).
- Nei file PHP, uniformare la struttura e aggiungere commenti per i traduttori.
- Versionare i file di traduzione separatamente.

## 5. Raccomandazioni
- Documenta sempre le regole di naming e struttura per i traduttori.
- Mantieni una checklist aggiornata per ogni ciclo di traduzione.
- Versiona i file di traduzione separatamente per facilitare il tracking delle modifiche.
